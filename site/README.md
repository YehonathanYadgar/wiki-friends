# Friend Group Wiki Site

Static HTML/CSS/JS export of the Obsidian-style wiki.

## Local Preview

Open `site/index.html` directly in a browser, or run:

```sh
python3 -m http.server 8080 -d site
```

## Netlify

Deploy the `site/` folder as the publish directory.

No build command is required after the site has been generated.
